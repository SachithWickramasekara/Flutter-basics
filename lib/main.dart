import 'package:flutter/material.dart';

void main(){
  runApp(
    const Center(
      child: Text(
        "This is my first flutter project",
        textDirection: TextDirection.rtl,
        )
      ),
  );
}